<% if current_user %>
    <%= link_to "log out", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %> 
    <br>
  <% end %>
<% @deals.each do |deal| %>
  <%= link_to "deal", home_path(deal) %> with user<%= deal.partner(current_user).id %> : <%= deal.sum(current_user) %> <%= button_to "delete", home_path(deal), method: :delete %>
  <br>
<% end %>
<br>
<%= link_to "start new deal", new_home_path %>
